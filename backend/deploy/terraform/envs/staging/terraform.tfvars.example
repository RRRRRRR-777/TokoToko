# ステージング環境の変数設定サンプル
#
# 推奨: Secret Managerを使用してパスワードを管理
# 1. Secret Managerにパスワードを保存:
#    PASSWORD=$(openssl rand -base64 32)
#    echo -n "$PASSWORD" | gcloud secrets create db-password-staging \
#      --data-file=- --replication-policy="automatic" --project=your-gcp-project-id
#
# 2. このファイルをコピーして terraform.tfvars を作成（パスワードなし）:
#    cp terraform.tfvars.example terraform.tfvars
#
# 3. 環境変数でパスワードを設定:
#    export TF_VAR_db_password=$(gcloud secrets versions access latest --secret="db-password-staging" --project=your-gcp-project-id)
#
# または、簡易的にterraform.tfvarsにパスワードを記載することも可能（非推奨）

# GCPプロジェクトID（必須）
project_id = "your-gcp-project-id"

# リージョン/ゾーン設定（オプション）
region = "asia-northeast1"
zone   = "asia-northeast1-a"

# Cloud SQLデータベースパスワード（必須）
# ⚠️ Secret Manager使用時はこの行をコメントアウトまたは削除
# db_password = "CHANGE_ME_STRONG_PASSWORD"
