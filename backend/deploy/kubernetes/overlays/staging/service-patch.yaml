# Staging環境用Service設定
# 静的外部IPを指定（Terraformで管理: tekutoko-api-staging-ip）
# TODO: terraform applyで静的IPを作成後、IPアドレスを設定
apiVersion: v1
kind: Service
metadata:
  name: tekutoko-api
spec:
  loadBalancerIP: __STAGING_STATIC_IP__
