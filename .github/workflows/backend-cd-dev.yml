name: Backend CD - Dev

on:
  push:
    branches:
      - dev-*
      - 'ticket/**'
    paths:
      - 'backend/**'
      - '.github/workflows/backend-cd-dev.yml'
  workflow_dispatch:

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: asia-northeast1
  GKE_CLUSTER: tekutoko-dev
  IMAGE_NAME: tekutoko-api
  GO_VERSION: '1.23'

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_DEV }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev

      - name: Build Docker image
        run: |
          docker build \
            --build-arg GO_VERSION=${{ env.GO_VERSION }} \
            -t ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/tekutoko/${{ env.IMAGE_NAME }}:${{ github.sha }} \
            -t ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/tekutoko/${{ env.IMAGE_NAME }}:dev-latest \
            -f Dockerfile .

      - name: Push Docker image
        run: |
          docker push ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/tekutoko/${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/tekutoko/${{ env.IMAGE_NAME }}:dev-latest

      - name: Output image digest
        id: image-digest
        run: |
          echo "digest=${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/tekutoko/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> $GITHUB_OUTPUT

    outputs:
      image-digest: ${{ steps.image-digest.outputs.digest }}

  deploy-to-gke:
    name: Deploy to GKE Dev
    runs-on: ubuntu-latest
    needs: build-and-push
    if: startsWith(github.ref, 'refs/heads/dev-')
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_DEV }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region=${{ env.GCP_REGION }} \
            --project=${{ env.GCP_PROJECT_ID }}

      - name: Update deployment image
        run: |
          kubectl set image deployment/tekutoko-api \
            api=${{ needs.build-and-push.outputs.image-digest }} \
            -n default

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/tekutoko-api -n default --timeout=5m

      - name: Verify deployment
        run: |
          kubectl get pods -n default -l app=tekutoko-api
          kubectl get services -n default tekutoko-api

      - name: Run smoke tests
        run: |
          EXTERNAL_IP=$(kubectl get service tekutoko-api -n default -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          echo "Testing health endpoint at http://$EXTERNAL_IP/health"
          curl -f http://$EXTERNAL_IP/health || exit 1

  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [build-and-push, deploy-to-gke]
    if: always()
    steps:
      - name: Notify success
        if: needs.deploy-to-gke.result == 'success'
        run: |
          echo "✅ Dev deployment successful"
          # TODO: Slack通知を追加

      - name: Notify failure
        if: needs.deploy-to-gke.result == 'failure'
        run: |
          echo "❌ Dev deployment failed"
          # TODO: Slack通知を追加
